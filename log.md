# log.md — 已做事项总账

> 记录「目前已经做了什么」。每次改动后按本格式在**最上方**追加一条（倒序，最新在前）。
>
> ```
> ## YYYY-MM-DD — 一句话概括
> **做了什么：** ...
> **在哪改的：** 文件 / 函数（供下次定位）
> **状态：** 已提交 / 待提交 / 待真机验证
> ```

---

## 2026-06-09 — 补修「视频分析后地图页 0 个景点」Bug

**做了什么：** 在 `/api/video/analyze` 去重后、写库与 return 之前补上坐标 / 类型补全循环，使接口响应与写库用同一份已补全数据，地图页不再因 `lat/lng` 为空被前端全部过滤。详见 `debug.md` 第 3 条。
**在哪改的：** `backend/routes/video.py` 的 `analyze()`。
**状态：** ⚠️ 待提交（git status 显示 `M backend/routes/video.py`）；仍需真机验证（跑通真实抖音链接看地图打点）。

---

## 2026-06-09 — 建立项目文档体系（CLAUDE.md / log.md）

**做了什么：** 新建 `CLAUDE.md`（项目背景 + 固定工作流）与本 `log.md`（已做事项总账）；`debug.md` 已先行存在（由三份原始改动日志合并、只抽真 Bug）。
**在哪改的：** 仓库根目录。
**状态：** 文档，本地保存。

---

## 截至 2026-06-09 的功能盘点（基线）

> 以下为合并三份历史日志 + 当前 `main` 分支代码核对后的已完成功能，作为总账起点。细节见 `debug.md`。

**核心功能**
- 旅行人格测试：`calculate_mbti()` 已重写为「6 人格最近原型匹配」（答案 → 维度向量 → 与 6 配方算欧氏距离），自动化测试 `tests/test_mbti_recipes.py` 全过。
- 抖音视频解析 → 景点提取 → 地图打点（坐标补全本次补齐）。
- 豆包 Responses API `web_search` 联网补全（pro 模型 + 原生 tools 参数；失败降级到 `DEFAULT_RECOMMEND_ATTRACTIONS`）。
- 行程规划：天数 / 预算数字输入，人均花费估算 + 预算对比徽章（`itinerary.py` `_estimate_cost()`）。
- 旅行搭子闭环：添加搭子 → 全流程展示头像 → 导出页「同步计划给搭子」（`POST /api/buddy/share-plan`）。
- 深度画像（选做）：结果页给结构化 prompt 让用户复制到常用 AI，粘回 JSON 存入 `deep_profile` 参与加权。
- 图文海报导出：引入 html2canvas，`exportAsImage()` 重写 + 路线缩略图本地 DOM/SVG 自绘。

**交互 / UI**
- 品牌改名：旅搭子 → 一拍迹合，✈️ → 🗺️。
- 问卷：排序题拖拽、滑块题 5 节点 + 未作答占位提示、底部「查看结果」+ 漏答高亮定位。
- 表头进度轴三列网格居中；每步骤「返回上一步」；老用户返回人格页空白已修（`ensureMbtiPageContent()`）。

**已知遗留**
- 视频景点入库 `city` 写死「上海」（`_save_locations_to_db()`），非上海城市走回库降级路径会查不到；兜底坐标表也只有上海数据。暂缓，详见 `debug.md` 第 7 条。
